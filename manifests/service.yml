apiVersion: v1
kind: Service
metadata:
    name: sirsamhainmessagesmicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sirsamhainmessagesmicroservice